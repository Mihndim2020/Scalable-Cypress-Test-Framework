# ============================================
# CI/CD Environment Template
# ============================================
# Use GitHub Actions Secrets for sensitive values
# This template shows what variables CI needs
# ============================================

# SECURITY: ALL SENSITIVE VALUES SHOULD BE SET VIA GITHUB SECRETS
# Never hardcode passwords, tokens, or API keys

# Application URLs (can be in .env or GitHub Secrets)
BASE_URL=${BASE_URL}  # Set via GitHub Secret
API_URL=${API_URL}    # Set via GitHub Secret

# Test Credentials
# SECURITY: Use ephemeral users generated at runtime
# DO NOT use hardcoded credentials in CI
TEST_USERNAME=${CYPRESS_USERNAME}  # GitHub Secret (fallback only)
TEST_PASSWORD=${CYPRESS_PASSWORD}  # GitHub Secret (fallback only)

# CI Environment Detection
CI=true
GITHUB_ACTIONS=true
NODE_ENV=test
ENVIRONMENT=ci

# Build Information (automatically set by GitHub Actions)
BUILD_NUMBER=${GITHUB_RUN_NUMBER}
RUN_ID=${GITHUB_RUN_ID}
BRANCH_NAME=${GITHUB_REF_NAME}
COMMIT_SHA=${GITHUB_SHA}

# Cypress Settings for CI
CYPRESS_VIDEO=true
CYPRESS_SCREENSHOTS=true
CYPRESS_baseUrl=${BASE_URL}

# Recording and Reporting
CYPRESS_RECORD_KEY=${CYPRESS_RECORD_KEY}  # GitHub Secret

# BrowserStack (Optional)
BROWSERSTACK_USERNAME=${BROWSERSTACK_USERNAME}  # GitHub Secret
BROWSERSTACK_ACCESS_KEY=${BROWSERSTACK_ACCESS_KEY}  # GitHub Secret

# Percy Visual Testing (Optional)
PERCY_TOKEN=${PERCY_TOKEN}  # GitHub Secret

# Slack Notifications (Optional)
SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}  # GitHub Secret

# Feature Flags
ENABLE_API_MOCKING=true
ENABLE_COVERAGE=true

# API Keys (Use Rotatable Keys)
# Generate these at runtime or use secrets manager
# API_KEY=${API_KEY}  # GitHub Secret

# Timeouts (CI needs higher values)
DEFAULT_COMMAND_TIMEOUT=15000
PAGE_LOAD_TIMEOUT=90000
